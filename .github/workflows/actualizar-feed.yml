name: Actualizar noticias diarias

on:
  schedule:
    - cron: "0 7 * * *" # Ejecuta cada día a las 07:00 UTC ≈ 08:00 Madrid
  workflow_dispatch:

permissions:
  contents: write

jobs:
  actualizar-feed:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4

      - name: Generar archivo XML actualizado
        run: |
          mkdir -p docs
          FECHA=$(TZ="Europe/Madrid" date +"%d/%m/%Y")
          PUBDATE=$(TZ="Europe/Madrid" date -R)

          echo '<?xml version="1.0" encoding="UTF-8"?>' > docs/noticias-diarias.xml.txt
          echo '<rss version="2.0">' >> docs/noticias-diarias.xml.txt
          echo '  <channel>' >> docs/noticias-diarias.xml.txt
          echo '    <title>Noticias Diarias - España</title>' >> docs/noticias-diarias.xml.txt
          echo '    <link>https://edekmm.github.io/Noticias-diarias/docs/noticias-diarias.xml.txt</link>' >> docs/noticias-diarias.xml.txt
          echo '    <description>Noticias destacadas en España, Madrid, economía mundial, economía española, construcción y Grupo ACS / Dragados.</description>' >> docs/noticias-diarias.xml.txt
          echo '    <language>es-es</language>' >> docs/noticias-diarias.xml.txt
          echo '    <item>' >> docs/noticias-diarias.xml.txt
          echo "      <title>Noticias del día ${FECHA}</title>" >> docs/noticias-diarias.xml.txt
          echo '      <description><
